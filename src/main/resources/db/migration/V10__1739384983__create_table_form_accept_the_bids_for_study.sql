CREATE TABLE if not exists form_accept_the_bids_for_study (
                                                       id SERIAL PRIMARY KEY,
                                                       mail_for_accept_bid VARCHAR(255),
                                                       meeting_date VARCHAR(255)
);
